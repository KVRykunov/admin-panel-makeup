let lext = "net";
if (5>3) {
    text="da"
}

alert ("5 more thet 3? + text "da"")


const tetrad = {
     name: "tetrad",
     weight: "300gr",
     pages: "100lisov",
     Oblojka: "solid",
     color : {
         face: "blue",
         inside: "yellow"
     }
}
if (tetrad.open) {
    tetrad.size = "150cm"
} else {
    tetrad.size = "75cm"
}


if (b) {
    Object.2=(2)}
else (a)
